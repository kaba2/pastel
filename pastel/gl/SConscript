from glob import glob

Import('env')
Import('config')

env = env.Clone()
config.apply('OpenGL', env)
config.apply('GLEW', env)

src = glob('*.cpp')

objs = []
for filename in src:
	objs += env.SharedObject(source=filename)

Return('objs')
