if (BuildExamples)
	#add_subdirectory ("PastelExample")

	# Create a solution folder for example projects.
	#set (ExampleSet)
	#set_property( TARGET ${ExampleSet} PROPERTY FOLDER "Examples")
endif()
